/**
 * Add Source Dialog - exports only the main dialog component
 * Internal sub-components (config-fields, direct-auth-fields, oauth-settings) are
 * implementation details not exported publicly.
 */

export { AddSourceDialog } from "./add-source-dialog";
