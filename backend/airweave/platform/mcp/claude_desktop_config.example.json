{
  "mcpServers": {
    "airweave": {
      "command": "mcp-proxy",
      "args": ["http://localhost:8001/mcp-server/mcp"],
      "env": {
        "AIRWEAVE_API_TOKEN": "your-api-token-here"
      },
      "description": "Airweave MCP Server - Make any app searchable for your agent"
    }
  }
}
